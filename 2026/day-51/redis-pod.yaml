apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    app: redis
    environment: dev
    team: platform
spec:
  containers:
    - name: redis-container
      image: redis:latest
      ports:
        - containerPort: 6379

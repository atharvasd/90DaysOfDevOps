apiVersion: v1
kind: Pod
metadata:
  name: resource-pod
spec:
  containers:
    - name: web-server
      image: nginx:1.25
      resources:
        requests:
          memory: "128Mi"
          cpu: "100m"
        limits:
          memory: "256Mi"
          cpu: "250m"
